<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.guet.mapper.UserMapper">
	<select id="login" resultType="user">
		SELECT userid as id,username FROM users WHERE username=#{username} AND password=#{password}
	</select>
	<select id="viewUserById" resultType="user">
		SELECT * FROM t_user WHERE userId=#{userId}
	</select>
	<update id="updateUser" parameterType="user">
		UPDATE t_user SET password=#{password},phone=#{phone},email=#{email} WHERE userId=#{userId}
	</update>


</mapper>